(()=>{
'use strict';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const st=document.createElement('style');st.textContent=`
.deepProto .dpStats{display:grid;grid-template-columns:repeat(4,1fr);gap:10px}.deepProto .dpStats>div,.deepProto .dpCard{border:1px solid var(--line);border-radius:12px;padding:13px;background:linear-gradient(180deg,var(--panel2),var(--panel))}.deepProto .dpStats b{display:block;color:var(--cyan);font-size:1.25rem}.deepProto .dpTabs{display:flex;gap:7px;flex-wrap:wrap;margin:12px 0}.deepProto button.dpTab{border:1px solid var(--line);background:#121824;color:#d9deea;border-radius:999px;padding:7px 11px;cursor:pointer}.deepProto button.dpTab.active{border-color:var(--cyan);color:#fff}.deepProto .dpForm{display:grid;grid-template-columns:minmax(160px,.7fr) minmax(140px,.5fr) 1fr auto;gap:9px;align-items:center;padding:9px 0;border-bottom:1px solid var(--line)}.deepProto .dpForm:last-child{border-bottom:0}.deepProto .dpPath{font-family:Consolas,monospace;font-size:.8rem;color:#aeb7cb}.deepProto .dpPlay{border:1px solid var(--line);background:#111827;color:#fff;border-radius:8px;padding:6px 9px;cursor:pointer}.deepProto .dpVerdict{padding:12px;border-left:3px solid var(--cyan);background:#0f1720;border-radius:8px}.deepProto .small{color:#aeb7cb}@media(max-width:800px){.deepProto .dpStats{grid-template-columns:1fr 1fr}.deepProto .dpForm{grid-template-columns:1fr}.deepProto .dpPlay{width:max-content}}
`;document.head.appendChild(st);
const sec=document.createElement('section');sec.className='section wrap deepProto';sec.id='deep-proto-words';sec.innerHTML=`<h2>Deep Proto-Word / Meaning Test</h2><p class="lead">The calibrated Indo-European test asks whether meaning survives known descent. This second-stage test goes one level higher: compare independently reconstructed family-level proto-words for the <b>same meanings</b> across Indo-European, Semitic, Austronesian, Uralic and Dravidian, then destroy the meaning labels and see whether the apparent similarity survives.</p><div class="notice"><b>Research boundary:</b> this is specifically designed to reject a false universal-word pattern. Same meaning across distant families must beat wrong meanings and shuffled labels before a deeper lexical ancestor is justified.</div><div id="dpStatus" class="status">Loading cross-family test…</div><div id="dpBody"></div>`;
const anchor=document.querySelector('#status')||document.querySelector('main')?.lastElementChild;if(anchor?.parentNode)anchor.parentNode.insertBefore(sec,anchor);else document.querySelector('main')?.appendChild(sec);
const nav=document.querySelector('.nav');if(nav&&!nav.querySelector('a[href="#deep-proto-words"]')){const a=document.createElement('a');a.href='#deep-proto-words';a.textContent='Deep words';nav.appendChild(a)}
let data,meaning='water',ctx,active=[];
function stop(){for(const n of active)try{n.stop()}catch(_){}active=[]}
function tone(cell,t,d=.19){if(!ctx)ctx=new (window.AudioContext||window.webkitAudioContext)();const row='ABCD'.indexOf(cell[0]),col=+cell[1]-1;const f=[260,330,420,520][col]*(1+row*.07),o=ctx.createOscillator(),g=ctx.createGain();o.type=row===0?'sine':row===1?'triangle':'sawtooth';o.frequency.value=f;g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(.07,t+.02);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(g).connect(ctx.destination);o.start(t);o.stop(t+d+.02);active.push(o)}
function playPath(p){stop();if(!ctx)ctx=new (window.AudioContext||window.webkitAudioContext)();ctx.resume();let t=ctx.currentTime+.04;for(const c of p){tone(c,t,.21);t+=.24}}
function render(){if(!data)return;const s=data.summary,x=data.meanings[meaning];const sig=s.operator_p_ge_observed<.05;const verdict=sig?'This pilot finds a cross-family same-meaning signal after operator alignment. It still requires a much larger independent concept set.':'This pilot does NOT find a reliable cross-family same-meaning signal. The family-level proto-forms are no more similar than shuffled meanings at this resolution, so a universal proto-word is not supported by this test.';document.querySelector('#dpStatus').className='status ok';document.querySelector('#dpStatus').textContent=`${data.method.proto_forms} family-level proto-forms · ${data.method.same_mean_cross_family_pairs} same-meaning cross-family pairs · ${data.method.permutations.toLocaleString()} shuffled controls`;
const stats=`<div class="dpStats"><div><b>${s.same_mean_identity.toFixed(3)}</b>same meaning, identity</div><div><b>${s.wrong_mean_identity.toFixed(3)}</b>wrong meaning, identity</div><div><b>${s.same_mean_operator_best.toFixed(3)}</b>same meaning, best operator</div><div><b>${s.operator_p_ge_observed.toFixed(3)}</b>operator permutation p</div></div>`;
const tabs=`<div class="dpTabs">${Object.keys(data.meanings).map(m=>`<button class="dpTab ${m===meaning?'active':''}" data-m="${esc(m)}">${esc(m)}</button>`).join('')}</div>`;
const forms=`<div class="dpCard"><h3>${esc(meaning)} — earliest family-level reconstructions tested</h3>${x.forms.map((f,i)=>`<div class="dpForm"><div><b>${esc(f.family)}</b><br><span class="small">${esc(f.source)}</span></div><div style="font-size:1.15rem">${esc(f.form)}</div><div class="dpPath">${esc(f.path.join(' → '))}</div><button class="dpPlay" data-play="${i}">▶ Play path</button></div>`).join('')}<p class="small">The medoid is the <i>existing family proto-form</i> with the smallest average bridge distance to the other families, not a reconstructed Proto-World word. Here: <b>${esc(x.medoid_family)} ${esc(x.medoid_form)}</b>, mean aligned similarity ${x.medoid_mean_similarity.toFixed(3)}.</p></div>`;
const explain=`<div class="dpCard"><h3>What this stage tests</h3><p>We first proved that established PIE words retain meaning-linked phonetic structure down known daughter branches. Here we remove that shared family history and ask whether independently reconstructed proto-families still converge for WATER, MOTHER, FIRE and STAR.</p><p>Identity comparison gives ${s.same_mean_identity.toFixed(3)} for the same meanings versus ${s.wrong_mean_identity.toFixed(3)} for wrong meanings (Δ ${s.identity_uplift>=0?'+':''}${s.identity_uplift.toFixed(3)}; shuffle p=${s.identity_p_ge_observed.toFixed(3)}). Allowing the eight fixed bridge operators gives ${s.same_mean_operator_best.toFixed(3)} versus ${s.wrong_mean_operator_best.toFixed(3)} (Δ ${s.operator_uplift>=0?'+':''}${s.operator_uplift.toFixed(3)}; shuffle p=${s.operator_p_ge_observed.toFixed(3)}).</p><div class="dpVerdict"><b>Current conclusion:</b> ${esc(verdict)}</div><p class="small">This is an early four-concept pilot. The next useful expansion is dozens of independently sourced basic meanings, family-balanced, with borrowing/sound-symbolism flags and held-out concepts.</p></div>`;
document.querySelector('#dpBody').innerHTML=stats+tabs+`<div style="display:grid;grid-template-columns:1.2fr .8fr;gap:14px">${forms}${explain}</div>`;document.querySelectorAll('.dpTab').forEach(b=>b.onclick=()=>{meaning=b.dataset.m;render()});document.querySelectorAll('[data-play]').forEach(b=>b.onclick=()=>playPath(x.forms[+b.dataset.play].path));}
fetch('data/phonetic-cross-family-proto-meaning.json?v=20260909-deep1',{cache:'no-store'}).then(r=>{if(!r.ok)throw new Error('HTTP '+r.status);return r.json()}).then(d=>{data=d;render()}).catch(e=>{const s=document.querySelector('#dpStatus');s.className='status error';s.textContent='Cross-family proto test unavailable: '+e.message});
})();