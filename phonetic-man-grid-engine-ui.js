(()=>{
'use strict';
if(window.__VARDATH_MAN_GRID_UI_V3__)return;window.__VARDATH_MAN_GRID_UI_V3__=true;
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),sleep=m=>new Promise(r=>setTimeout(r,m));
let api=null,spec=null,last=null,cells=new Map();
function getState(){try{return typeof state!=='undefined'?state:null}catch{return null}}
async function ready(){for(let i=0;i<300;i++){const s=getState();if(s?.loaded&&s.parameters?.size)return s;await sleep(100)}throw Error('PHOIBLE did not finish loading')}
function css(){if($('#manGridEngineStyles'))return;const s=document.createElement('style');s.id='manGridEngineStyles';s.textContent=`.manugrid .mg-shell{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(300px,.65fr);gap:16px;align-items:start}.manugrid .mg-panel{background:linear-gradient(180deg,var(--panel2),var(--panel));border:1px solid var(--line);border-radius:15px;padding:16px;min-width:0}.manugrid .mg-workbench{overflow:auto;max-height:76vh;background:#ece7da;border:1px solid #404861;border-radius:12px;padding:14px}.manugrid .mg-model{width:max-content;min-width:100%;display:flex;flex-direction:column;align-items:center;gap:12px;color:#17130f}.manugrid .mg-layer{text-align:center}.manugrid .mg-title{font:700 11px Consolas,monospace;margin-bottom:5px}.manugrid .mg-pair{display:grid;grid-template-columns:max-content 18px max-content;gap:7px;align-items:stretch}.manugrid .mg-side{font:700 9px Consolas,monospace;margin-bottom:3px}.manugrid .mg-spine{width:2px;background:#37312b;justify-self:center}.manugrid .mg-grid{display:grid;grid-template-columns:repeat(var(--c),var(--cell));gap:1px;--cell:18px}.manugrid .mg-cell{width:var(--cell);height:var(--cell);padding:0;border:1px solid #665f56;background:#faf7ef;color:#5d554c;border-radius:1px;font:7px Consolas,monospace;cursor:pointer}.manugrid .mg-cell.hitL{background:#95e3e5;color:#071015}.manugrid .mg-cell.hitR{background:#d2a8e8;color:#13071b}.manugrid .mg-cell.hitBoth{background:#9fd6a9;color:#07150b}.manugrid .mg-cell.hitParent{box-shadow:inset 0 0 0 3px #ff9f43}.manugrid .mg-cell.selected{outline:3px solid #ffcf66;z-index:3}.manugrid .mg-circle{width:105px;height:105px;border-radius:50%;border:3px solid #3c3731;background:radial-gradient(circle,#f8f4e9 0 29%,#d7d0c1 30% 47%,#f8f4e9 48% 61%,#bdb4a4 62% 64%,#eee8db 65%);font:800 10px Consolas,monospace}.manugrid textarea,.manugrid select{width:100%;background:#0c1018;color:white;border:1px solid #3d455d;border-radius:9px;padding:9px}.manugrid textarea{min-height:70px}.manugrid .mg-buttons{display:flex;flex-wrap:wrap;gap:7px;margin:10px 0}.manugrid .mg-note{border-left:4px solid var(--cyan);padding:11px 13px;background:#0e171b;border-radius:8px;margin:12px 0}.manugrid .mg-status{font:12px Consolas,monospace;color:var(--cyan);margin:7px 0}.manugrid .mg-results{max-height:48vh;overflow:auto}.manugrid .mg-kv{display:grid;grid-template-columns:1fr auto;gap:10px;padding:7px 0;border-bottom:1px solid var(--line)}.manugrid .mg-kv span:last-child{color:var(--cyan);font-family:Consolas,monospace}.manugrid .mg-chip{display:inline-block;margin:3px;padding:3px 7px;border:1px solid var(--line);border-radius:999px;background:#0d1018;font-family:Georgia,serif}.manugrid .mg-cellinfo{margin-top:10px;padding:10px;border:1px solid var(--line);border-radius:10px;background:#0d121a}.manugrid .mg-legend{display:flex;gap:8px;flex-wrap:wrap;font-size:.76rem;margin:7px 0}.manugrid .mg-swatch{display:inline-block;width:10px;height:10px;border:1px solid #777;margin-right:4px;vertical-align:-1px}@media(max-width:900px){.manugrid .mg-shell{grid-template-columns:1fr}}@media(max-width:600px){.manugrid .mg-grid{--cell:13px}.manugrid .mg-cell{font-size:0}.manugrid .mg-pair{grid-template-columns:max-content 12px max-content;gap:5px}}`;document.head.appendChild(s)}
function grid(l,side){let h='';for(let r=1;r<=l.rows;r++)for(let c=1;c<=l.columns;c++){const id=`${l.id}-${side}-R${r}-C${c}`;h+=`<button class="mg-cell" data-cell="${id}" title="${id}">${r}.${c}</button>`}return`<div class="mg-grid" style="--c:${l.columns}">${h}</div>`}
function layer(l){return`<div class="mg-layer"><div class="mg-title">${l.id} · ${l.columns}×${l.rows} · each side</div><div class="mg-pair"><div><div class="mg-side">LEFT</div>${grid(l,'L')}</div><div class="mg-spine"></div><div><div class="mg-side">RIGHT</div>${grid(l,'R')}</div></div></div>`}
function clear(){cells=new Map();document.querySelectorAll('#mgModel .mg-cell').forEach(b=>{b.className='mg-cell';b.style.opacity=''})}
function mark(id,k,e){const b=document.querySelector(`#mgModel [data-cell="${CSS.escape(id)}"]`);if(!b)return;const r=cells.get(id)||{L:[],R:[],parent:[]};r[k].push(e);cells.set(id,r);b.classList.toggle('hitBoth',r.L.length>0&&r.R.length>0);b.classList.toggle('hitL',r.L.length>0&&!r.R.length);b.classList.toggle('hitR',r.R.length>0&&!r.L.length);b.classList.toggle('hitParent',r.parent.length>0);b.style.opacity=String(.55+.45*Math.min(1,(r.L.length+r.R.length+r.parent.length)/8))}
function map(m,k,e){for(const x of m.all)mark(x.cell,k,e)}
function center(){const v=$('#mgViewport'),m=$('#mgModel');if(v&&m)requestAnimationFrame(()=>v.scrollLeft=Math.max(0,(m.scrollWidth-v.clientWidth)/2))}
function inspect(id){document.querySelectorAll('#mgModel .mg-cell').forEach(b=>b.classList.toggle('selected',b.dataset.cell===id));const r=cells.get(id)||{L:[],R:[],parent:[]},chips=a=>a.slice(0,60).map(x=>`<span class="mg-chip">/${esc(x.name||'?')}/</span>`).join('')||'none';$('#mgCellInfo').innerHTML=`<b>${esc(id)}</b> · mirror <b>${esc(api.mirrorCell(id)||'—')}</b><div><b>LEFT:</b> ${chips(r.L)}</div><div><b>RIGHT:</b> ${chips(r.R)}</div><div><b>PARENT:</b> ${chips(r.parent)}</div>`}
const srcInv=()=>$('#sourceInventory')?.value||'',tarInv=()=>$('#targetInventory')?.value||'',srcPhone=()=>api.parameterById($('#sourceSegment')?.value)||null;
function sound(){clear();const p=srcPhone();if(!p)return;const L=api.mapParameter(p,'L'),R=api.mapParameter(p,'R');map(L,'L',{name:p.Name});map(R,'R',{name:p.Name});last={type:'sound',ipa:p.Name,left:L,right:R};$('#mgResults').innerHTML=`<h3>Mapped sound /${esc(p.Name)}/</h3><div class="mg-kv"><span>coordinate</span><span>x ${L.xy.x.toFixed(4)} · y ${L.xy.y.toFixed(4)}</span></div><div class="mg-kv"><span>states</span><span>${L.all.length} per side</span></div>`;center()}
function seq(){clear();const t=api.tokenizeIPA($('#mgSequence').value),side=$('#mgSide').value,m=[];for(const p of t.tokens){const z=api.mapParameter(p,side);m.push(z);map(z,side,{name:p.Name})}last={type:'sequence',side,tokens:m.map(x=>({ipa:x.name,cells:x.all.map(c=>c.cell)})),unknown:t.unknown};$('#mgResults').innerHTML=`<h3>Mapped IPA sequence</h3><div>${m.map(x=>`<span class="mg-chip">/${esc(x.name)}/</span>`).join('')}</div><div class="mg-kv"><span>parsed</span><span>${m.length}</span></div><div class="mg-kv"><span>unmatched</span><span>${t.unknown.length?esc(t.unknown.join(' ')):'0'}</span></div>`;center()}
function inv(which){clear();const side=which==='source'?'L':'R',r=api.inventoryAnalysis(which==='source'?srcInv():tarInv(),side);for(const z of r.segments)map(z,side,{name:z.name});const occ=new Set(r.segments.flatMap(z=>z.all.map(c=>c.cell)));last={type:'inventory',which,inventory:r.inventory_id,mapped:r.mapped,occupied:occ.size};$('#mgResults').innerHTML=`<h3>${which} inventory mapped</h3><div class="mg-kv"><span>segments</span><span>${r.mapped}</span></div><div class="mg-kv"><span>occupied states</span><span>${occ.size}</span></div>`;center()}
function compare(){clear();const r=api.compareInventories(srcInv(),tarInv());for(const z of r.pairs){map(z.source_map,'L',{name:z.source.Name});map(z.target_map,'R',{name:z.target.Name})}last={type:'compare',source:r.source_inventory,target:r.target_inventory,mean_feature_distance:r.mean_feature_distance,layer_stats:r.layer_stats,pairs:r.pairs.map(z=>({source:z.source.Name,target:z.target.Name,distance:z.distance}))};$('#mgResults').innerHTML=`<h3>Sibling-channel comparison</h3><div class="mg-kv"><span>paired sounds</span><span>${r.pairs.length}</span></div><div class="mg-kv"><span>mean feature distance</span><span>${r.mean_feature_distance?.toFixed(4)??'—'}</span></div>${Object.entries(r.layer_stats).map(([k,v])=>`<div class="mg-kv"><span>${k} same mirrored coordinate</span><span>${(100*v.rate).toFixed(1)}%</span></div>`).join('')}`;center()}
function parent(){clear();const r=api.inferParent(srcInv(),tarInv());for(const z of r.pairs){map(api.mapParameter(z.left,'L'),'L',{name:z.left.Name});map(api.mapParameter(z.right,'R'),'R',{name:z.right.Name});if(z.parent){map(z.parent_map_L,'parent',{name:z.parent.Name});map(z.parent_map_R,'parent',{name:z.parent.Name})}}const better=r.mean_child_pair_distance!=null&&r.deterministic_mismatch_mean!=null?r.mean_child_pair_distance<r.deterministic_mismatch_mean:null;last={type:'parent',source:r.source_inventory,target:r.target_inventory,pair_count:r.pair_count,mean_child_pair_distance:r.mean_child_pair_distance,mismatch:r.deterministic_mismatch_mean,candidates:r.candidate_parent_inventory.map(x=>x.ipa)};$('#mgResults').innerHTML=`<h3>Candidate common-parent inventory</h3><div class="mg-kv"><span>paired child sounds</span><span>${r.pair_count}</span></div><div class="mg-kv"><span>candidate parent sounds</span><span>${r.candidate_parent_inventory.length}</span></div><div class="mg-kv"><span>real child-pair distance</span><span>${r.mean_child_pair_distance?.toFixed(4)??'—'}</span></div><div class="mg-kv"><span>mismatch control</span><span>${r.deterministic_mismatch_mean?.toFixed(4)??'—'}</span></div><div class="mg-kv"><span>real beats mismatch</span><span>${better==null?'—':better?'YES':'NO'}</span></div><div>${r.candidate_parent_inventory.slice(0,80).map(x=>`<span class="mg-chip">/${esc(x.ipa)}/</span>`).join('')}</div>`;center()}
function exportJSON(){const p={version:3,tool:'Vardath Man Grid phonetic engine',spec,projection:api.projection,analysis:last},b=new Blob([JSON.stringify(p,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='vardath-man-grid-analysis.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
async function init(){css();try{const [r,st]=await Promise.all([fetch('data/man-grid-structure-v1.json?v=20260911-structure2',{cache:'no-store'}),ready()]);spec=await r.json();api=window.VARDATH_MAN_GRID_CORE.create(spec,st)}catch(e){console.error(e);return}const old=$('#manuscript-grid');if(old)old.remove();const s=document.createElement('section');s.id='manuscript-grid';s.className='section wrap manugrid';s.innerHTML=`<div class="eyebrow">Exact computerized Man Grid · phonetic engine</div><h2>The Man Grid — working phonetic tool</h2><p class="lead">The grid now computes. PHOIBLE feature vectors are mapped into a reproducible phonetic coordinate, quantized through every exact Man Grid resolution, mirrored LEFT↔RIGHT, compared between language inventories, and used to infer candidate common-parent sounds.</p><div class="mg-note"><b>Grid:</b> mirrored 4×4, 7×5, 10×5, 7×4, 5×5 pairs → transformative circle → mirrored 6×5, 11×5, 17×4, 21×5, 25×5 pairs. <b>${spec.counts.total_cells.toLocaleString()} cells + circle.</b></div><div class="mg-note"><b>Mapping rule:</b> two data-derived principal components of the full PHOIBLE distinctive-feature vectors provide the continuous coordinate. Each Man Grid layer quantizes that same sound at its own resolution. Mirroring changes sibling side only.</div><div class="mg-shell"><div class="mg-panel"><div class="mg-legend"><span><i class="mg-swatch" style="background:#95e3e5"></i>LEFT/source</span><span><i class="mg-swatch" style="background:#d2a8e8"></i>RIGHT/target</span><span><i class="mg-swatch" style="background:#9fd6a9"></i>same coordinate</span><span><i class="mg-swatch" style="background:#fff;border:3px solid #ff9f43"></i>parent</span></div><div class="mg-workbench" id="mgViewport"><div id="mgModel" class="mg-model">${spec.upper.map(layer).join('')}<button id="mgCircle" class="mg-circle">TRANSFORMATIVE<br>CIRCLE</button>${spec.lower.map(layer).join('')}</div></div><div class="mg-status">PHOIBLE loaded · ${getState().parameters.size.toLocaleString()} segment types · computational grid ready</div></div><div class="mg-panel"><label>IPA / phoneme sequence</label><textarea id="mgSequence" placeholder="k a t  or  kat"></textarea><label>Sequence side</label><select id="mgSide"><option value="L">LEFT sibling channel</option><option value="R">RIGHT sibling channel</option></select><div class="mg-buttons"><button class="btn" id="mgSound">Map selected phoneme</button><button class="btn" id="mgSeq">Map IPA sequence</button><button class="btn" id="mgSourceInv">Map source inventory</button><button class="btn" id="mgTargetInv">Map target inventory</button><button class="btn" id="mgCompare">Compare source ↔ target</button><button class="btn" id="mgParent">Infer common parent</button><button class="btn" id="mgClear">Clear</button><button class="btn" id="mgExport">Export JSON</button></div><div id="mgResults" class="mg-results"><p class="small">Use the existing Source/Target language and inventory selectors above, then run an analysis.</p></div><div id="mgCellInfo" class="mg-cellinfo"><b>Cell inspector</b><div class="small">Click a cell after an analysis to inspect its sounds and mirror address.</div></div></div></div><div class="mg-note"><b>Batch API:</b> <code>VARDATH_MAN_GRID_API</code> exposes the same mapping, inventory comparison, parent inference and mirror operations for the reconstruction tests.</div>`;const m=$('#mirror-lab');if(m?.parentNode)m.parentNode.insertBefore(s,m);else(document.querySelector('main')||document.body).appendChild(s);$('#mgModel').onclick=e=>{const b=e.target.closest('.mg-cell');if(b)inspect(b.dataset.cell)};$('#mgSound').onclick=sound;$('#mgSeq').onclick=seq;$('#mgSourceInv').onclick=()=>inv('source');$('#mgTargetInv').onclick=()=>inv('target');$('#mgCompare').onclick=compare;$('#mgParent').onclick=parent;$('#mgClear').onclick=()=>{clear();last=null;$('#mgResults').innerHTML='<p class="small">Cleared.</p>'};$('#mgExport').onclick=exportJSON;window.VARDATH_MAN_GRID_SPEC=spec;window.VARDATH_MAN_GRID_API=api;document.dispatchEvent(new CustomEvent('vardath-man-grid-ready',{detail:{spec,api}}));center()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(init,80),{once:true});else setTimeout(init,80);
})();