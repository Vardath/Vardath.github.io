const INTEGRATED_CELLS=['A1','A2','A3','A4','B1','B2','B3','B4','C1','C2','C3','C4','D1','D2','D3','D4'];
let BENCH=null;
const gi=id=>document.getElementById(id);
function ie(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function featureWords(p){return FEATURE_NAMES.filter(f=>fnum(p[f])>.25)}

// Canonical isolated IPA reference recordings hosted by Wikimedia Commons.
const IPA_AUDIO_FILES={
'p':'Voiceless bilabial plosive.ogg','b':'Voiced bilabial plosive.ogg','t':'Voiceless alveolar plosive.ogg','d':'Voiced alveolar plosive.ogg','k':'Voiceless velar plosive.ogg','g':'Voiced velar plosive.ogg','ɡ':'Voiced velar plosive.ogg','m':'Bilabial nasal.ogg','n':'Alveolar nasal.ogg','ŋ':'Velar nasal.ogg','ɲ':'Palatal nasal.ogg','f':'Voiceless labiodental fricative.ogg','v':'Voiced labiodental fricative.ogg','θ':'Voiceless dental fricative.ogg','ð':'Voiced dental fricative.ogg','s':'Voiceless alveolar sibilant.ogg','z':'Voiced alveolar sibilant.ogg','ʃ':'Voiceless postalveolar fricative.ogg','ʒ':'Voiced postalveolar fricative.ogg','x':'Voiceless velar fricative.ogg','ɣ':'Voiced velar fricative.ogg','h':'Voiceless glottal fricative.ogg','l':'Alveolar lateral approximant.ogg','r':'Alveolar trill.ogg','ɹ':'Alveolar approximant.ogg','j':'Palatal approximant.ogg','w':'Voiced labio-velar approximant.ogg','ʔ':'Glottal stop.ogg','i':'Close front unrounded vowel.ogg','y':'Close front rounded vowel.ogg','ɨ':'Close central unrounded vowel.ogg','ʉ':'Close central rounded vowel.ogg','ɯ':'Close back unrounded vowel.ogg','u':'Close back rounded vowel.ogg','ɪ':'Near-close near-front unrounded vowel.ogg','ʊ':'Near-close near-back rounded vowel.ogg','e':'Close-mid front unrounded vowel.ogg','ø':'Close-mid front rounded vowel.ogg','ɘ':'Close-mid central unrounded vowel.ogg','ɵ':'Close-mid central rounded vowel.ogg','ɤ':'Close-mid back unrounded vowel.ogg','o':'Close-mid back rounded vowel.ogg','ə':'Mid-central vowel.ogg','ɛ':'Open-mid front unrounded vowel.ogg','œ':'Open-mid front rounded vowel.ogg','ɜ':'Open-mid central unrounded vowel.ogg','ɞ':'Open-mid central rounded vowel.ogg','ʌ':'Open-mid back unrounded vowel.ogg','ɔ':'Open-mid back rounded vowel.ogg','æ':'Near-open front unrounded vowel.ogg','ɐ':'Near-open central vowel.ogg','a':'Open front unrounded vowel.ogg','ɶ':'Open front rounded vowel.ogg','ɑ':'Open back unrounded vowel.ogg','ɒ':'Open back rounded vowel.ogg'};
function audioReferenceFor(segment){const raw=String(segment||'').normalize('NFD');if(IPA_AUDIO_FILES[raw])return{symbol:raw,file:IPA_AUDIO_FILES[raw],exact:true};const stripped=raw.replace(/[\u0300-\u036f\u1ab0-\u1aff\u1dc0-\u1dff\u20d0-\u20ff\uFE20-\uFE2Fːˑʰʷʲˠˤʼ̥̬̩̯̚͜͡]/gu,'');const chars=Array.from(stripped);if(chars.length===1&&IPA_AUDIO_FILES[chars[0]])return{symbol:chars[0],file:IPA_AUDIO_FILES[chars[0]],exact:false};return null}
function commonsAudioUrl(file){return'https://commons.wikimedia.org/wiki/Special:Redirect/file/'+encodeURIComponent(file)}
window.playIpaReference=function(symbol,button){const ref=audioReferenceFor(symbol);if(!ref)return;if(window.__ipaAudio){try{window.__ipaAudio.pause()}catch(_){}}const a=new Audio(commonsAudioUrl(ref.file));window.__ipaAudio=a;const old=button?.textContent;if(button)button.textContent='▶ playing';const reset=()=>{if(button)button.textContent=old||'🔊 hear'};a.addEventListener('ended',reset,{once:true});a.addEventListener('error',()=>{if(button){button.textContent='audio unavailable';button.disabled=true}},{once:true});a.play().catch(()=>{if(button){button.textContent='tap again';setTimeout(reset,1800)}})};
function audioButton(segment){const ref=audioReferenceFor(segment);if(!ref)return'';const label=ref.exact?'hear':'reference /'+ref.symbol+'/';return`<button type="button" data-ipa="${ie(segment)}" onclick="playIpaReference(this.dataset.ipa,this)" title="Wikimedia Commons IPA reference recording${ref.exact?'':' — base sound only'}" style="margin-left:7px;border:1px solid #3d455d;background:#0d1320;color:#eef1f8;border-radius:999px;padding:5px 9px;cursor:pointer;font-size:.76rem">🔊 ${ie(label)}</button>`}

// Real language-specific word audio from WikIPA (WikiPron IPA + Lingua Libre speech).
const WIKIPA_DATASET='pierluigic/WikIPA',HF_API='https://datasets-server.huggingface.co';
let WIKIPA_META=null,WIKIPA_REQUEST=0;
function ensureRealSpeechPanel(){if(gi('realSpeech'))return;const corr=gi('correspondence');if(!corr)return;const sec=document.createElement('div');sec.id='realSpeech';sec.className='panel';sec.style.marginTop='22px';sec.innerHTML=`<h3>Hear the sounds inside real words</h3><p class="small">WikIPA pairs human-curated WikiPron IPA with real Lingua Libre recordings. This lets you compare the isolated IPA reference above with an actual speaker using the sound inside a word. Coverage is currently limited to the languages present in WikIPA.</p><div id="realSpeechStatus" class="status">Preparing WikIPA real-speech examples…</div><div id="realSpeechGrid" class="experiment"></div><p class="small">Source: <a href="https://huggingface.co/datasets/pierluigic/WikIPA" target="_blank" rel="noopener">WikIPA</a>. Recordings originate from Lingua Libre/Wikimedia and may contain speaker/accent variation — that variation is useful evidence, not an error.</p>`;corr.appendChild(sec)}
async function hfJSON(path,params){const u=new URL(HF_API+path);Object.entries(params).forEach(([k,v])=>v!==undefined&&v!==null&&u.searchParams.set(k,v));const r=await fetch(u,{cache:'force-cache'});if(!r.ok)throw Error('WikIPA API '+r.status);return r.json()}
function rowObj(x){return x?.row||x||{}}
function candidateField(names,keys){const low=keys.map(k=>[k,k.toLowerCase()]);for(const n of names){const exact=low.find(([,l])=>l===n);if(exact)return exact[0]}for(const n of names){const part=low.find(([,l])=>l.includes(n));if(part)return part[0]}return null}
function findDeepString(v,pred,depth=0){if(depth>5||v==null)return null;if(typeof v==='string')return pred(v)?v:null;if(Array.isArray(v)){for(const x of v){const q=findDeepString(x,pred,depth+1);if(q)return q}}else if(typeof v==='object'){for(const [k,x] of Object.entries(v)){if(typeof x==='string'&&pred(x,k))return x;const q=findDeepString(x,pred,depth+1);if(q)return q}}return null}
function audioUrlFromRow(r){const direct=findDeepString(r,(s,k='')=>/^https?:\/\//.test(s)&&(/\.(ogg|wav|mp3|flac|m4a)(\?|$)/i.test(s)||/audio|resolve|media/i.test(k)));if(direct)return direct;const path=findDeepString(r,(s,k='')=>/audio|path|file/i.test(k)&&/\.(ogg|wav|mp3|flac|m4a)$/i.test(s));if(path&&/^https?:\/\//.test(path))return path;return null}
function textVal(r,key){const v=key?r[key]:null;if(v==null)return'';if(typeof v==='string'||typeof v==='number')return String(v);return findDeepString(v,s=>typeof s==='string')||''}
async function initWikipaMeta(){if(WIKIPA_META)return WIKIPA_META;const splits=await hfJSON('/splits',{dataset:WIKIPA_DATASET});const ss=(splits.splits||[]).find(x=>x.split==='train')||(splits.splits||[])[0];if(!ss)throw Error('No WikIPA split available');const first=await hfJSON('/first-rows',{dataset:WIKIPA_DATASET,config:ss.config,split:ss.split});const firstRow=rowObj((first.rows||[])[0]);const keys=Object.keys(firstRow);WIKIPA_META={config:ss.config,split:ss.split,lang:candidateField(['language','lang','language_code','iso6393','iso','code'],keys),word:candidateField(['word','text','orthography','term','grapheme'],keys),ipa:candidateField(['ipa','pronunciation','transcription','phonetic'],keys),keys,firstRows:first.rows||[]};return WIKIPA_META}
function normLang(s){return String(s||'').toLowerCase().replace(/[_-].*$/,'').trim()}
async function fetchWikipaLanguage(lang){const meta=await initWikipaMeta(),iso=String(lang?.ISO||'').toLowerCase(),name=String(lang?.Name||'');let rows=[];const attempts=[];if(meta.lang&&iso)attempts.push(`"${meta.lang.replace(/"/g,'""')}"='${iso.replace(/'/g,"''")}'`);if(meta.lang&&name)attempts.push(`"${meta.lang.replace(/"/g,'""')}"='${name.replace(/'/g,"''")}'`);for(const where of attempts){try{const j=await hfJSON('/filter',{dataset:WIKIPA_DATASET,config:meta.config,split:meta.split,where,length:60,offset:0});rows=(j.rows||[]).map(rowObj);if(rows.length)break}catch(_){}}
if(!rows.length&&iso){try{const j=await hfJSON('/search',{dataset:WIKIPA_DATASET,config:meta.config,split:meta.split,query:iso,length:100,offset:0});rows=(j.rows||[]).map(rowObj)}catch(_){}}
if(meta.lang&&(iso||name)){rows=rows.filter(r=>{const v=normLang(textVal(r,meta.lang));return !v||v===iso||v===normLang(name)||v.includes(iso)})}return rows}
function speechCard(title,lang,rows,needle){const meta=WIKIPA_META;if(!meta)return'';let items=rows.map(r=>({r,word:textVal(r,meta.word),ipa:textVal(r,meta.ipa),audio:audioUrlFromRow(r)}));if(needle){const n=String(needle).normalize('NFC');const hit=items.filter(x=>String(x.ipa).normalize('NFC').includes(n));if(hit.length)items=hit}items=items.filter(x=>x.audio||x.word||x.ipa).slice(0,4);return`<div class="panel"><h4>${ie(title)} · ${ie(lang?.Name||'')}</h4>${items.length?items.map(x=>`<div style="border-top:1px solid var(--line);padding:10px 0"><div><b>${ie(x.word||'word')}</b> <span class="pill">/${ie(x.ipa||'IPA unavailable')}/</span></div>${x.audio?`<audio controls preload="none" src="${ie(x.audio)}" style="width:100%;margin-top:7px"></audio>`:'<div class="small">Audio URL was not exposed by the public dataset viewer for this row.</div>'}</div>`).join(''):`<p class="small">No matching WikIPA word recording was returned for this language/sound.</p>`}</div>`}
async function renderRealSpeech(){ensureRealSpeechPanel();if(!state?.loaded||!gi('realSpeechGrid'))return;const token=++WIKIPA_REQUEST,status=gi('realSpeechStatus');status.className='status';status.textContent='Looking up real speaker recordings in WikIPA…';const sLang=state.languages.get(gi('sourceLanguage')?.value),tLang=state.languages.get(gi('targetLanguage')?.value),sp=state.parameters.get(gi('sourceSegment')?.value);const tgtIds=state.inventorySegments.get(gi('targetInventory')?.value)||[],best=sp?nearest(sp,tgtIds,false).ranked[0]:null;try{const [sr,tr]=await Promise.all([fetchWikipaLanguage(sLang),fetchWikipaLanguage(tLang)]);if(token!==WIKIPA_REQUEST)return;status.className='status ok';status.innerHTML=`Real-speech layer ready · WikIPA/Lingua Libre${sp?` · looking for /${ie(sp.Name)}/ and /${ie(best?.p?.Name||'')}/ inside words`:''}`;gi('realSpeechGrid').innerHTML=speechCard('Source speech',sLang,sr,sp?.Name)+speechCard('Target speech',tLang,tr,best?.p?.Name)}catch(e){if(token!==WIKIPA_REQUEST)return;status.className='status error';status.innerHTML=`WikIPA real-speech lookup could not load in this browser: ${ie(e.message)}. The isolated Commons IPA buttons above still work; <a href="https://huggingface.co/datasets/pierluigic/WikIPA" target="_blank" rel="noopener">open the WikIPA dataset</a>.`;gi('realSpeechGrid').innerHTML=''}}

function renderSoundCorrespondence(){if(!state?.loaded)return;const srcInv=gi('sourceInventory')?.value,tgtInv=gi('targetInventory')?.value;if(!srcInv||!tgtInv)return;const src=(state.inventorySegments.get(srcInv)||[]).map(id=>state.parameters.get(id)).filter(Boolean).filter(p=>classify(p)!=='PROSODY');const tgtIds=state.inventorySegments.get(tgtInv)||[];const sLang=state.languages.get(gi('sourceLanguage').value),tLang=state.languages.get(gi('targetLanguage').value);gi('corrTitle').textContent=`${sLang?.Name||'Source'} → ${tLang?.Name||'Target'}`;const rows=src.map(p=>{const r=nearest(p,tgtIds,false).ranked[0];if(!r)return'';const q=r.p,sc=classify(p),tc=classify(q),shared=featureWords(p).filter(x=>featureWords(q).includes(x));const dif=r.parts.slice(0,6).map(x=>x[0]);let why=`Both sounds share ${shared.slice(0,6).join(', ')||'few strongly positive features'}. `;why+=sc===tc?`They occupy the same coarse bridge cell (${sc}), so their broad articulatory state agrees. `:`They occupy different bridge cells (${sc} → ${tc}), so this is a cross-cell fallback. `;if(dif.length)why+=`The largest remaining PHOIBLE differences are ${dif.join(', ')}.`;const kind=p.Name===q.Name?'exact IPA':(sc===tc?'same-cell':'fallback');return`<div class="corrrow"><div class="corrsound">/${ie(p.Name)}/${audioButton(p.Name)} <span>→</span> /${ie(q.Name)}/${audioButton(q.Name)}</div><div class="corrmeta"><span class="pill">${ie(sc)}</span><span class="pill">${ie(tc)}</span><span class="pill">distance ${r.distance.toFixed(4)}</span><span class="pill ${kind==='exact IPA'?'good':''}">${kind}</span></div><details><summary>Why this maps this way</summary><p>${ie(why)}</p><p class="small">🔊 Isolated buttons are canonical Wikimedia Commons IPA references. The real-word panel below uses WikIPA/Lingua Libre speaker recordings where available.</p></details></div>`}).join('');gi('correspondenceGrid').innerHTML=rows||'<p class="small">No comparable non-prosodic sounds in these inventories.</p>';renderRealSpeech()}

function renderBenchmark(){if(!BENCH)return;const c=BENCH.counts,core=BENCH.core_thresholds['0.8']||[],strict=BENCH.core_thresholds['1.0']||[];gi('benchStatus').className='status ok';gi('benchStatus').innerHTML=`Complete benchmark · ${c.datasets_completed}/${c.wikipron_datasets_indexed} WikiPron datasets · ${c.canonical_languages} canonical languages · ${c.pairwise_comparisons.toLocaleString()} pairwise comparisons · ${c.datasets_failed} failures`;gi('benchStats').innerHTML=`<span><b>${c.canonical_languages}</b>languages</span><span><b>${core.length}</b>80% core gates</span><span><b>${strict.length}</b>100% strict core</span><span><b>${c.families_ge_3_languages}</b>families ≥3 languages</span>`;gi('benchCore').innerHTML=core.map(g=>`<span class="pill">${ie(g)}</span>`).join(' ')||'<span class="small">none</span>';let html='<div></div>'+INTEGRATED_CELLS.map(x=>`<div class="bmhead">${x}</div>`).join('');const by=new Map(BENCH.gate_prevalence.map(x=>[x.index,x]));for(let i=0;i<16;i++){html+=`<div class="bmhead">${INTEGRATED_CELLS[i]}</div>`;for(let j=0;j<16;j++){const g=by.get(i*16+j),p=g?.prevalence||0;html+=`<div class="bmcell" title="${ie(g?.gate||'')} · ${(p*100).toFixed(1)}% languages · power ratio 3^${g?.power_exponent_delta??0}" style="background:rgba(92,225,230,${(.06+.86*p).toFixed(3)})">${(p*100).toFixed(0)}%</div>`}}gi('benchmarkMatrix').innerHTML=html;const top=[...BENCH.power_delta_profile].sort((a,b)=>b.language_prevalence-a.language_prevalence||b.mass-a.mass).slice(0,12);gi('powerProfile').innerHTML=top.map(x=>`<div class="metric"><b>3^${x.delta}</b><span>${(x.language_prevalence*100).toFixed(1)}% of languages</span></div>`).join('')}
async function loadBenchmark(){try{const r=await fetch('data/phonetic-benchmark-summary.json?v=2',{cache:'no-store'});if(!r.ok)throw Error('HTTP '+r.status);BENCH=await r.json();renderBenchmark()}catch(e){gi('benchStatus').className='status error';gi('benchStatus').textContent='Benchmark results unavailable: '+e.message}}
function wireIntegrated(){ensureRealSpeechPanel();['sourceLanguage','targetLanguage','sourceInventory','targetInventory','sourceSegment'].forEach(id=>gi(id)?.addEventListener('change',()=>setTimeout(()=>{renderSoundCorrespondence();renderRealSpeech()},0)));const timer=setInterval(()=>{if(typeof state!=='undefined'&&state.loaded){clearInterval(timer);renderSoundCorrespondence();renderRealSpeech()}},250);loadBenchmark()}
document.addEventListener('DOMContentLoaded',wireIntegrated);
