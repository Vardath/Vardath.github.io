name: Inject Phonetic Concept Evidence
on:
  push:
    paths:
      - 'phonetic-concept-evidence.js'
      - '.github/workflows/inject-phonetic-concept-evidence.yml'
permissions:
  contents: write
jobs:
  inject:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Inject concept evidence script
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('phonetic-bridge.html')
          s=p.read_text(encoding='utf-8')
          tag='<script defer src="phonetic-concept-evidence.js?v=20260913-concept1"></script>'
          if tag not in s:
              s=s.replace('</head>',tag+'\n</head>')
              p.write_text(s,encoding='utf-8')
          PY
      - name: Commit wiring
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add phonetic-bridge.html
          git commit -m 'Show concept-level sound meaning evidence on phonetics page'
          git push
